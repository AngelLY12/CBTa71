---
interface Props {
    href: string;
    title: string;
    active?: boolean;
}

const { href, title, active = false } = Astro.props;
---

<a
    href={href}
    class={`hover:font-normal hover:text-blue-700 ${active && "text-blue-700 underline"}`}
    >{title}</a
>
